import styles from './CatalogPage.module.css';

const CatalogPage = () => {
    return(
        <div className={`${styles.catalog_page} container`}>
            <h1>Catalog</h1>
        </div>
    );
}

export default CatalogPage;